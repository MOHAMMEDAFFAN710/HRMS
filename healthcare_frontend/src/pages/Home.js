import React from "react";
import "../styles/App.css"; // Ensure styles are linked

const Home = () => {
    return (
        <div className="home-container">
            <h1>Welcome To The Healthcare App</h1>
            <p>Your health, our priority.</p>
        </div>
    );
};

export default Home;
